DEV/DEMO ONLY. Remove password-based demo login end-to-end and replace it with a secure development-only one-click account chooser for the four fixed demo identities: Demo Admin, Hardy, Mason, and Wayne.

Requirements:
1. UI: remove the password field completely. Replace email/password form with four compact role/account buttons or cards: Continue as Demo Admin, Hardy, Mason, Wayne. Keep the current compact light Franklin design and mobile responsiveness.
2. Server: implement a dedicated DEV-only demo-login endpoint accepting only a fixed allowlisted account identifier (not arbitrary email, role, or user id). It must create the existing server-side session and return the same safe session payload.
3. Hard safety gate: the endpoint and any passwordless bootstrap/provisioning must refuse unless NODE_ENV is development/test AND an explicit opt-in env/config flag is enabled. Production must return 404/403 and must never allow role selection or impersonation.
4. Provision/bootstrap the four fixed DEV identities idempotently without password secrets. Preserve exact intended roles: admin=owner_admin, Hardy=manager, Mason/Wayne=onboarding_specialist. Do not create duplicate memberships.
5. Preserve existing logout, session cookies, CSRF/security behavior, unauthenticated protection, server-side RBAC, owner filtering, privacy masking, and all other auth behavior.
6. Remove the four FRANKLINS_DEMO_*_PASSWORD requirements from the DEV demo path only. Do not expose or read secret values. Do not weaken any production auth code path.
7. Add/update focused tests for: all four allowlisted logins, invalid account rejection, arbitrary role/email rejection, production gate rejection, unauthenticated protection, logout, and role isolation/cross-owner denial.
8. Run API and frontend tests, typechecks, build, diff check. Restart only existing DEV API/web workflows and visually inspect the actual preview.
9. Do not modify real driver/compliance data, Google Sheets, external systems, production, Hello World, franklins.ai, publishing, republishing, or deployment.
10. Finish with exact changed files, test totals, non-sensitive account/role results, and DEV preview link.

Work directly in the existing project. Do not create a separate mockup or planning task.