Continue autonomously and make the Recruiting system fully ready for the owner by morning. Treat this as the final implementation/QA pass, not a planning-only task. Do NOT start unrelated suggested task #17 unless it is directly required by the acceptance criteria below. Existing Onboarding UI, routes, data, and behavior must remain unchanged.

1) First perform a read-only inventory of the normal development database and current migrations. Then apply ONLY the checked-in additive Recruiting migration to the normal development database using the project migration mechanism. No drops, truncates, destructive rewrites, or deletion of any existing row/object. Verify the migration is idempotent and existing Onboarding regressions remain green.
2) If the migrated Recruiting tables contain no cases, add a clearly DEV/DEMO-only, repeatable seed command and seed a realistic trucking recruiting dataset sufficient to exercise every major UI state. It must be idempotent, never run automatically in production, and must not alter existing Onboarding data. Include multiple recruiters/managers, SLA green/amber/red, manager review, future follow-up, drug test, compliance docs, contract, ready-for-onboarding, closed-lost, and duplicate-protection examples.
3) Make the live Recruiting UI fully functional against the normal API/database: Work Queue, All Cases, Pipeline/Kanban, Manager Review, War Room, New Case, Case Detail/timeline, legal stage transitions, manager approve/reject handoff, Future Follow-up return, Closed Lost with reason/note, and exact-once transfer to Onboarding. Every mutation must enforce owner, task owner, next action, deadline, SLA, optimistic version, stable idempotency key, workspace security, and duplicate prevention.
4) Fix all loading/auth/role/error/empty states so the user never sees Unknown/No Role or ERROR LOADING WORK QUEUE in the normal Franklin demo session. Preserve role-gated recruiter/manager navigation and provide clear recovery messages for genuine API errors.
5) Run full typecheck/build, all Recruiting domain/repository/API/PostgreSQL tests, Manager Board/Onboarding regressions, migration verification, and real-browser end-to-end tests at 320, 768, 1024, and 1440. Test every main page and at least one successful Manager Review, Future Follow-up, Closed Lost, duplicate rejection, and exact-once hire transfer flow. Check console/network errors and accessibility basics.
6) Polish the UI to a fast, modern, professional trucking operations product without changing the established Franklin design language. No placeholder controls, fake buttons, dead links, demo defaults inside user forms, horizontal overflow, or components over the project limit.
7) Leave the normal web and API workflows running on the final code and normal development database. Do not publish or attach a paid/custom domain yet. Produce a concise completion report with migration applied, seed status, test counts/results, browser evidence, remaining risks (if any), and the exact preview path. Keep working through errors and independent review until every acceptance item is green; do not stop at a partial milestone or ask routine questions.