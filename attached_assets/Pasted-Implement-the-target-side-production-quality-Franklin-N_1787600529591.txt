Implement the target-side production-quality Franklin NEW LEAD ingest slice in this Driver-Onboarding-Portal project. Do not publish/deploy, do not change Google Sheets, and do not mutate existing production driver/case data during verification.

Context: the separate franklins.ai source now sends one Ready-to-process driver server-to-server to POST {RECRUITING_BASE_URL}/api/leads with Authorization: Bearer {secret}, Content-Type application/json, and stable Idempotency-Key franklins.ai:recruiting:new-lead:v1:{source_lead_id}. The payload is server-derived and contains: source_system='franklins.ai', source_tenant='Franklin', source_lead_id, external_id='franklins.ai:lead:{id}', driver_name, normalized phone, driver_type ('owner_operator'|'company_driver'), documents {cdl_front, cdl_back, medical_card}, docs_received, displayed_recruiter, requested_by {account_id, full_name}, requested_at.

First inspect the existing RecruitingCase domain/API/schema/stage/event/owner conventions and reuse them. Then implement:
1. Exact authenticated endpoint POST /api/leads, protected only by a server secret env named FRANKLINS_RECRUITING_INGEST_TOKEN. Fail closed with 503 if not configured; 401 for missing/bad bearer. Never log token or full phone.
2. Strict Zod schema: reject extra fields and invalid identity/type/phone/timestamps with 422. Accept only source_system franklins.ai and source_tenant Franklin.
3. Durable idempotency at DB level, additive migration only. Unique source identity and idempotency key; persist canonical sorted-key payload hash and original request payload. Same key + same payload/identity => 200 {ok:true,status:'already_exists',target_lead_id?}; new => 201 {ok:true,status:'created',target_lead_id}. Same key/source identity with different canonical payload => 409 {ok:false,error:'idempotency_conflict'}. Handle concurrent requests safely.
4. Create exactly one Franklin Recruiting case in the existing domain at the actual NEW LEAD initial stage/lifecycle, with driver fields/doc flags/source metadata and immutable audit/event. Do not create an Onboarding driver. Do not modify existing Sheet-synced cases. Use the existing server-enforced owner/task/SLA rules; if intake assignment is required, use the existing unassigned/system intake convention and preserve displayed_recruiter as source metadata, not trusted authorization.
5. Add a NEW LEADS section below the current Hardy/Mason/Wayne grouped board that shows only cases created by this API and not yet claimed/advanced. Match the current professional layout, 7 visible rows with internal vertical mouse-wheel scrolling, phone/SMS actions, useful columns (driver/contact/type/docs received/CDL/MED/source recruiter/received time), and no second extra board. Existing three sections and Onboarding stay unchanged.
6. When a user claims or advances a NEW LEAD through the existing case action, it must disappear from NEW LEADS and enter the normal Recruiting workflow exactly once. Do not invent an unsafe direct mutation path; reuse existing server transition/capability rules.
7. Add route-level HTTP tests with fake DB/target boundary for auth, strict validation, 201, duplicate 200, canonical reordered JSON, conflict 409, concurrency, migration missing, and no Onboarding/source mutation. Add UI tests for NEW LEADS filtering/render/7-row internal scroll and disappearance after advancement. Run typecheck, targeted tests, full non-DB tests, builds, and a non-destructive browser pass. Never click a live mutating action.

Report exact files, migration, env/config values still required, exact test counts, and any real blocker. Do not claim end-to-end live until migration, secret, source base URL, and deployment are configured.