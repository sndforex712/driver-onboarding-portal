Increment 1 is approved after review. Begin Increment 2A only: additive Recruiting database foundation and migration safety, using RED -> GREEN. Do not implement routes, repository services, frontend, import UI, scheduler, integrations, or change existing Onboarding/Manager Board behavior. First perform a read-only preflight and report existing schema/migration conventions plus duplicate/orphan risks; do not mutate existing rows. Introduce a real checked-in migration path—never use drizzle push-force and do not apply destructive/backfill operations. Add the minimal core tables needed for the next transaction increment: recruiting_cases; append-only recruiting_case_events; recruiting_transition_effects (or equivalent durable per-effect idempotency ledger); recruiting_onboarding_transfers exact-once ledger. Model RecruitingCase as a hiring attempt linked to durable driver/lead/workspace; workspace membership-backed Case Owner and Task Owner; stage/lifecycle; next action/deadline; SLA deadline; follow-up due/resume stage; structured closed-lost reason/note; optimistic version; transfer status/timestamps. Enforce database constraints where practical: workspace-scoped case number/source ID uniqueness, one active RecruitingCase per driver/workspace, active required fields, follow-up fields, closed-lost reason, unique transition/effect keys, one transfer per RecruitingCase and unique transfer idempotency key, target onboarding IDs nullable until success. Prevent cross-workspace owner references via composite constraints if the existing schema supports it; otherwise document and test the required transaction-layer check for Increment 2B. Add an additive nullable unique recruiting_case_id link on onboarding_cases only if it can be done without backfill or existing behavior changes; otherwise defer with a documented reason. Use TDD/schema tests: prove constraint definitions and generated migration first, include rollback/down migration if the repository format supports it, and test that existing tables/files are preserved. Do not apply the migration to production or destroy demo data. Run focused tests, existing Recruiting/Manager Board tests, db/API/workspace typecheck, build, and git diff check. Report exact files/SQL, RED evidence, GREEN results, preflight findings, and anything deferred. Stop after 2A for review.